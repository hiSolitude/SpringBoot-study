#cupSize: B
#age: 18
#context: "cupSize: ${cupSize},age: ${age}"  #在属性配置中调用自己的属性spring:spring:
spring:
  profiles:
    active: dev
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/dbgril
    username: root
    password: 123456
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
